<Window x:Class="ZTHubApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Paramètres" Height="250" Width="450" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    
    <Grid Background="{StaticResource PrimaryBackground}">
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="URL de base de Zone-Telechargement:"/>
            <TextBox Grid.Row="1" x:Name="UrlBox" Margin="0,5,0,15"/>

            <Label Grid.Row="2" Content="Clé API AllDebrid:"/>
            <TextBox Grid.Row="3" x:Name="ApiKeyBox" Margin="0,5,0,15"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="Sauvegarder" IsDefault="true" Width="100" Margin="0,0,10,0" Click="SaveButton_Click"/>
                <Button Content="Annuler" IsCancel="true" Width="80"/>
            </StackPanel>
        </Grid>
        
    </Grid>
</Window>