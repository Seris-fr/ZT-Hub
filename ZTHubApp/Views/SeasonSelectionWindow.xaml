<Window x:Class="ZTHubApp.Views.SeasonSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sélection des saisons"
        Height="500"
        Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e"
        Foreground="White"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3d3d3d"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3d3d3d"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                   x:Name="TitleTextBlock"
                   Text="Sélectionnez les saisons à télécharger"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   TextWrapping="Wrap"/>

        <!-- Info sur les saisons déjà téléchargées -->
        <TextBlock Grid.Row="1"
                   x:Name="InfoTextBlock"
                   Text=""
                   FontSize="11"
                   Foreground="#4A9EFF"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <!-- Liste des saisons avec CheckBox -->
        <Border Grid.Row="2"
                BorderBrush="#3d3d3d"
                BorderThickness="1"
                Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="SeasonsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#2d2d2d"
                                    BorderThickness="0,0,0,1"
                                    Padding="5">
                                <CheckBox Content="{Binding DisplayText}"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          IsEnabled="{Binding IsEnabled}"
                                          Foreground="{Binding TextColor}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Boutons de sélection rapide -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10">
            <Button Content="Tout cocher"
                    Width="120"
                    Click="SelectAll_Click"/>
            <Button Content="Tout décocher"
                    Width="120"
                    Click="DeselectAll_Click"/>
            <Button Content="Cocher manquantes"
                    Width="140"
                    Click="SelectMissing_Click"/>
        </StackPanel>

        <!-- Boutons d'action -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Télécharger"
                    Width="110"
                    Click="Download_Click"
                    IsDefault="True"/>
            <Button Content="Annuler"
                    Width="110"
                    Click="Cancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
